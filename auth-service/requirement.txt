fastapi
uvicorn
sqlmodel
passlib[bcrypt]
# Trava em <4.1: passlib 1.7.4 quebra ao detectar o backend do bcrypt em
# versões mais novas da lib bcrypt (erro "password cannot be longer than 72
# bytes" ao simplesmente gerar qualquer hash — nada a ver com o tamanho da
# senha real). https://github.com/pyca/bcrypt/issues/684
bcrypt==4.0.1
python-jose[cryptography]
pytest
pytest-cov
